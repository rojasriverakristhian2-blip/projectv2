# ============================================
# Variables de Entorno - Gestión del Conocimiento
# ============================================
# 
# Copia este archivo a .env y ajusta los valores según tu entorno
# cp ENV.example .env
#
# ============================================
# Base de Datos MySQL
# ============================================
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=gestionconocimiento
DB_USERNAME=admin
DB_PASSWORD=admin
MYSQL_PORT=3306

# ============================================
# Spring Boot Backend
# ============================================
# URL de conexión a la base de datos (se construye automáticamente si usas docker-compose)
SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/gestionconocimiento?useSSL=false&serverTimezone=UTC
SPRING_DATASOURCE_USERNAME=admin
SPRING_DATASOURCE_PASSWORD=admin

# Configuración JPA
SPRING_JPA_HIBERNATE_DDL_AUTO=update
SPRING_JPA_SHOW_SQL=true

# ============================================
# CORS Configuration
# ============================================
# URLs permitidas separadas por comas (sin espacios)
# Para producción, incluye las URLs de tu frontend desplegado
CORS_ALLOWED_ORIGINS=http://localhost:4200,http://localhost:80,http://localhost

# ============================================
# Notas:
# ============================================
# - Los valores después de :- son valores por defecto en docker-compose.yml
# - Para producción, cambia las contraseñas y URLs
# - No subas el archivo .env al repositorio (debe estar en .gitignore)

